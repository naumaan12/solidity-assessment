{
  "name": "smart-contracts-tech-assessment",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "description": "Tech assessment smart contracts coding challenge",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf ./build ./artifacts ./cache",
    "test": "hardhat test",
    "test:stack": "set HARDHAT_SHOW_STACK_TRACES=true&& hardhat test",
    "compile": "hardhat compile",
    "deploy": "hardhat run scripts/deploy.js --network test",
    "lint": "solhint --config ./.solhint.json contracts/**/*.sol"
  },
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "chai": "^4.3.4",
    "chai-as-promised": "^7.1.1",
    "hardhat": "^2.28.6",
    "lodash": "^4.17.21",
    "nodemon-gulp": "^3.1.15",
    "rimraf": "^5.0.0",
    "web3": "^1.10.4"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
